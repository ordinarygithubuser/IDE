import { Action } from 'mva';

export const CreateSession = Action();
export const CloseSession = Action();
export const SelectSession = Action();

export const SetInput = Action();
export const Execute = Action();
export const Previous = Action();
export const Next = Action();