import { Action } from 'mva';

export const OpenFile = Action();
export const CloseFile = Action();
export const SelectFile = Action();

export const SetContext = Action();
export const Save = Action();
export const SetText = Action();